mwu_dev_module.add('mk_tabs', function() {

    'use strict';

    // var ui = mysli.js.ui,
    //     panel = new ui.panel('mysli-cms-dash-tabs'),
    //     titlebar = new ui.titlebar({color: 'default'});

    // titlebar.push(new ui.button({
    //     icon: 'close',
    //     style_flat: true
    // })).connect('click', function () {
    //     panel.destroy();
    // });
    // titlebar.push(new ui.title("Tabs Examples"), true);

    // panel.front.push(titlebar);

    // return panel;
});
