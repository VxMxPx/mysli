// Generated by CoffeeScript 1.8.0
(function() {
  var __hasProp = {}.hasOwnProperty,
    __extends = function(child, parent) { for (var key in parent) { if (__hasProp.call(parent, key)) child[key] = parent[key]; } function ctor() { this.constructor = child; } ctor.prototype = parent.prototype; child.prototype = new ctor(); child.__super__ = parent.prototype; return child; };

  mysli.web.ui.title = (function(_super) {
    var template, ui;

    __extends(title, _super);

    ui = mysli.web.ui;

    template = '<span class="ui-widget ui-title" />';

    function title(text, options) {
      if (options == null) {
        options = {};
      }
      if (typeof text === 'string') {
        options.text = text;
      } else {
        options = text;
      }
      this.options = ui.util.merge_options(options, {
        text: null,
        level: 1
      });
      title.__super__.constructor.call(this, this.options);
      this.elements.push($(template));
      ui.util.apply_options(this.options, this, {
        set_level: 'level',
        set_text: 'text'
      });
    }


    /*
    Set title level 1...6
    @param {integer} level
     */

    title.prototype.set_level = function(level) {
      this.options.level = level;
      this.get_element().html($("<h" + level + "/>"));
      return this.set_text(this.options.text);
    };


    /*
    Get current level
    @returns {integer}
     */

    title.prototype.get_level = function() {
      return this.options.for_id;
    };


    /*
    Set label's text
    @param {string} text
     */

    title.prototype.set_text = function(text) {
      this.options.text = text;
      return this.get_element().find(':first-child').text(text);
    };


    /*
    Get label's text
    @returns {string}
     */

    title.prototype.get_text = function() {
      return this.options.text;
    };

    return title;

  })(mysli.web.ui.widget);

}).call(this);
